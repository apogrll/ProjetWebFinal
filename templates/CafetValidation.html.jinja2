{% extends "MasterCafet.html.jinja2" %}
{% block body %}

    <div class="row">
        <button class="btn btn-lg btn-block" disabled> {{message}} </button>
    </div>
    <h1> </h1>
    <a href="{{ url_for("viewcafet_produits") }}" class="btn btn-lg btn-secondary btn-block btn-light" class="bottom"> Retour à la liste des produits</a>

{% endblock %}